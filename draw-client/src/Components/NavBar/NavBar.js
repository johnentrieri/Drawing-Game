import React from 'react';

const navBar = (props) => {
    return (
        <nav className="navbar navbar-dark bg-dark">
            <span className="navbar-brand mb-0 h1">Online Drawing Game</span>
        </nav>
    );
};

export default navBar;